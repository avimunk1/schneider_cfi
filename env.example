# Frontend environment variables
# Copy this to .env and configure based on your environment

# =============================================================================
# LOCAL DEVELOPMENT
# =============================================================================

# Leave empty - Vite proxy will handle /api requests to localhost:8000
VITE_API_BASE_URL=

# Enable test features for quick layout testing with existing images
VITE_SHOW_TEST_FEATURES=true

# Google AI API key (for demo page image generation)
VITE_GOOGLE_GENAI_API_KEY=your_key_here

# =============================================================================
# PRODUCTION (Vercel)
# =============================================================================

# Set to your Railway backend URL (no trailing slash)
# Example: VITE_API_BASE_URL=https://schneider-backend.railway.app

# Disable test features in production
# VITE_SHOW_TEST_FEATURES=false

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# Vercel Configuration:
# 1. Set VITE_API_BASE_URL to your Railway backend URL
# 2. Set VITE_SHOW_TEST_FEATURES=false
# 3. vercel.json handles SPA routing
# 4. .vercelignore excludes backend/

# Railway Configuration (Backend):
# 1. Root Directory: backend
# 2. Required env vars: GOOGLE_GENAI_API_KEY, OPENAI_API_KEY, ALLOWED_ORIGIN, ASSETS_PATH
# 3. ALLOWED_ORIGIN should be your Vercel domain
# 4. Create Volume: Mount at /app/assets (1GB+)
# 5. Set ASSETS_PATH=/app/assets
# 6. Set ADMIN_TOKEN for downloading analytics/logs (generate random secure token)

